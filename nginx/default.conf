# ─────────────────────────────────────────────────────────────────────────────
# DEPRECATED — Docker-era Nginx config
#
# This file was used when the app ran inside Docker Compose.
# It proxied to the `web` container over TCP (http://web:8000).
#
# For VPS (Gunicorn + systemd) deployment, use:
#   deploy/nginx/medburg_crm.conf
#
# This file is kept for reference only and should NOT be installed on the VPS.
# It will be removed when Docker support is fully retired.
# ─────────────────────────────────────────────────────────────────────────────

# server {
#     listen 80;
#     client_max_body_size 20M;
#
#     location /static/ {
#         alias /app/staticfiles/;
#     }
#
#     location / {
#         proxy_pass http://web:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }